# USER.md

No human talks to this agent. It is spawned non-interactively by the skill-builder
eval harness (`eval_core.run_functional`), one turn per trial. There is nobody to
learn about — leave this empty.
