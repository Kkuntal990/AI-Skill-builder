You are improving an OpenClaw skill description to make it trigger more reliably.

The current description FAILED to trigger on these user messages (an LLM judge
picked a different skill or "none"). Rewrite the description so the right
messages trigger it, without becoming so broad it triggers on unrelated ones.

Rules:
- Keep it ONE paragraph (3-5 sentences).
- Write in THIRD PERSON (no "I"/"you"/"we"). The description is injected into the
  system prompt for skill selection; inconsistent point-of-view breaks discovery.
- Start with an action verb ("Train...", "Apply...", "Generate...").
- State BOTH what the skill does AND when to use it. Include at least one "Use when..."
  OR "Invoke when..." clause with concrete scenarios from the failing messages, plus the
  key terms (class / algorithm names) a user would actually mention.
- Be a little "pushy" — agents tend to UNDER-trigger skills. A clause like "Use this
  whenever the user works with <X>, even if they don't explicitly ask for it" raises recall.
- Cut BOTH failure modes at once: rewrite so the failing messages now trigger (recall),
  WITHOUT becoming so broad it fires on unrelated tasks (precision). If the judge picked
  another skill because the description over-claimed someone else's territory, narrow that claim.
- Name specific capabilities (class names, algorithm names) — not generic terms.
- Don't list keywords separated by commas ("lora, qlora, sft"). Write prose.
- Don't claim capabilities the skill doesn't have — stay within what the body describes.
- Keep it under ~900 characters (hard cap 1024 — over 1024 rejects the build, and the listing truncates it). Compact and specific beats long and exhaustive.

Return ONLY the new description as a single plain text paragraph. No quotes,
no markdown, no prose before/after.

---
SKILL NAME: {{skill_name}}

CURRENT SKILL.md BODY (for capability reference):
{{skill_body}}

CURRENT DESCRIPTION:
{{current_description}}

FAILING USER MESSAGES (these SHOULD have triggered the skill):
{{failing_prompts}}

JUDGE'S REASONING (why other skills won):
{{judge_reasons}}
