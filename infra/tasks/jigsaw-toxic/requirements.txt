# Task-level Python deps for jigsaw-toxic (deferred follow-up pilot).
#
# Intentionally empty as of 2026-05-25 — transformers/peft/accelerate/
# datasets/bitsandbytes/sentencepiece/scikit-learn are in the base image
# lockfile. See infra/tasks/llama-inference/requirements.txt for rationale.
#
# NOTE for the PEFT follow-up: if jigsaw-toxic needs trl (transformers
# RL/SFT trainer), pin to a pre-1.4 release (trl 1.4+ wants
# transformers>=4.56 which conflicts with our vllm-pinned 4.53.1). Better
# path: bump the base image's VLLM_TAG to a newer release that supports
# transformers 5.x, then add `trl` to requirements.in instead of here.
