# Task-level dependencies for house-prices.
# Installed at trajectory startup by entrypoint.sh into a PVC-cached pip dir.
# Both with_skill and without_skill cells get these libraries (so the only
# difference between cells is the SKILL.md prompt injection, not library
# availability). Pinned versions for reproducibility.

lightgbm==4.5.0
xgboost==2.1.3
catboost==1.2.7
